import {initAndRender} from '../app';
import {Chapter2_HashTableFunctions} from '../chapter2_hash_table_functions.js';

if (typeof window !== 'undefined') {
    initAndRender([Chapter2_HashTableFunctions]);
}
